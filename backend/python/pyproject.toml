[project]
name = "proposal-brief-builder-backend"
version = "0.1.0"
description = "Backend evidence engine for Proposal Brief Builder"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["."]
